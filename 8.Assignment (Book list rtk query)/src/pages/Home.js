import BookListHeader from '../components/home/BookListHeader';
import BookList from '../components/home/BookList';

const Home = () => {
	return (
		<>
			<BookListHeader />
			<BookList/>
		</>
	);
};

export default Home;
